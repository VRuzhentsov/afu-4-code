import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vruzhentsov.afu4code',
  appName: 'AFU-4-code',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
